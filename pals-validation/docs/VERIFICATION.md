# 本地验收记录（2026-09-18）

完成范围：无 GPU 的代码测试、合成管线测试、真实数据预处理。没有连接 B1、调用模型、下载权重或产生新科学实验结果。

`unittest discover -s tests -v`：26 项全部通过。`pip wheel --no-deps --no-build-isolation .`：安装包构建通过。构建不等于真实推理后端验收。

真实输入 118 题：115 条 final DAG、3 条经过 accepted 诊断的 candidate fallback。源文件哈希见 MIGRATION.md。

| 预处理项目 | 适用题数 |
|---|---:|
| 原轨迹固定首步相邻破坏 | 97 |
| forest 基线相邻破坏 | 104 |
| 合法完整子树交换 | 44 |
| 同 forest 基线合法／破坏配对 | 44 |
| E2 单 hash 锚点祖先续写 | 104 |

不适用题保留在 inventory/selection 中。以上是设计适用数，不是模型成功输出数，也不是效果证明。

测试覆盖：图合法性和共享前缀、目标选择、token 对齐索引、负支持与标准差公式、解析停止边界、共同目标比较、输入篡改拒绝、重复输出拒绝、E1/E2 mock 完整运行与续跑、未完成运行拒绝验收。

以上是 GPU canary 之前的本地检查记录。后续 Qwen2.5-7B 真实八卡检查见 [CANARY-20260918.md](CANARY-20260918.md)。其他模型兼容性、全量吞吐及格式通过率仍未验证。Mock 数值明确标记为非科学证据。
