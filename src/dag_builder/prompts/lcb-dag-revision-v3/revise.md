V3 input compactness note: the full frozen program is represented losslessly
by the ordered `C` source units. It is deliberately not repeated as a separate
reference_code field. The prior graph is a compact *diagnostic sketch* with
source spans and the duplicate answer-code attachment removed; this does not
change the original question, program, explanation or frozen CPU evidence.
Do not infer correctness from this sketch. Reconstruct only source-grounded
claims and explicit reasoning. Keep the new step list concise enough to leave
room for final JSON output; put unresolved bridges in omissions instead of
consuming the budget on speculative reasoning.
