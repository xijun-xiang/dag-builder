V3 note: the dependency stage receives the complete normalized nodes and
source material. Base parent edges on those actual nodes, not the compact
earlier diagnostic sketch. Every indispensable parent must be declared.
