V4 is still a same-model review of one newly revised candidate, not an
independent ground truth. Judge the full frozen task and code on their merits,
without rewarding repair of the old formatting symptom. In particular,
check whether the program actually works at maximum stated input sizes and
whether a claimed greedy, monotone, recurrence, set or search theorem is
proved rather than merely asserted. Try boundary and counterexample inputs.
Every retained step and edge must be justified; a false statement, missing
necessary premise, wrong algorithm or unacceptable worst-case runtime means
reject or needs_review, not accept. Never say a diagnostic proof of an
incorrect program is an accepted reference DAG. Do not silently change the
code or infer correctness from the frozen CPU test pass.
